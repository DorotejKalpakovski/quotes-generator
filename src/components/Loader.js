const Loader = () => {
  return (
    <div className="loader-container center-screen">
      <div className="spinner"></div>
      <p>Loading please wait.</p>
    </div>
  );
};

export default Loader;
